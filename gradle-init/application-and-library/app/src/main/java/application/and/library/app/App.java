/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package application.and.library.app;

import application.and.library.list.LinkedList;

import static application.and.library.utilities.StringUtils.join;
import static application.and.library.utilities.StringUtils.split;
import static application.and.library.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
